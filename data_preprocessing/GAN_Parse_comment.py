# metaData.clear()
				# flag = True
				# for subline in line[1].split(","):
				# 	# print subline
				# 	if "originalUrl" in subline:
				# 		subline = subline.replace('originalUrl','').strip()
				# 		# print subline[4:len(subline)-1]
				# 		if 'originalUrl' in metaData:
				# 			metaData['originalUrl'].append(subline[4:len(subline)-1])
				# 	elif "inlineUrl" in subline:
				# 		subline = subline.replace('inlineUrl','').strip()
				# 		# print subline[4:len(subline)-1]
				# 		if 'inlineUrl' in metaData:
				# 			metaData['inlineUrl'].append(subline[4:len(subline)-1])
				# 	elif "imageUrlHash" in subline:
				# 		subline = subline.replace('imageUrlHash','').strip()
				# 		# print subline[4:len(subline)-1]
				# 		if 'imageUrlHash' in metaData:
				# 			metaData['imageUrlHash'].append(subline[4:len(subline)-1])
				# 	elif "linkType" in subline:
				# 		subline = subline.replace('linkType','').strip()
				# 		# print subline[4:len(subline)-1]
				# 		if 'linkType' in metaData:
				# 			metaData['linkType'].append(subline[4:len(subline)-1])
				# 	elif "title" in subline:
				# 		subline = subline.replace('title','').strip()
				# 		# print subline[4:len(subline)-1]
				# 		if 'title' in metaData:
				# 			metaData['title'].append(subline[4:len(subline)-1])
				# 	elif "desc" in subline:
				# 		subline = subline.replace('desc','').strip()
				# 		# print subline[4:len(subline)-1]
				# 		if 'desc' in metaData:
				# 			metaData['desc'].append(subline[4:len(subline)-1])
